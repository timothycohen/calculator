tests checked:
  styling:
    never breaks on resizing
    text never overflows and is always readable
    styling is consistent

  logic:
    provides correct answers
    clicking the buttons and the keyboard do the same thing
    backspace button clears input when held and backspaces on click
    does not allow invalid character input
    cannot input multiple operators
    cannot start with an operator
    can put a negative sign on both numbers
    operators can be switched if they are the most recent input
    does not allow division by 0
    can put only one decimal point on each of the two operands
    rounds if necessary to 5 decimal points